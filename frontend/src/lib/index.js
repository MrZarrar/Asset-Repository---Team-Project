/**
 * @fileoverview Library entrypoint - place files to import through the `$lib` alias in this folder.
 * @module lib/index
 * 
 * This directory contains shared utilities, services, and components that can be imported
 * throughout the application using the $lib alias.
 * 
 * @example
 * // Import a service from lib:
 * import { fetchAssets } from '$lib/assetService';
 * 
 * // Import authentication functionality:
 * import { login, logout } from '$lib/auth';
 */

// place files you want to import through the `$lib` alias in this folder.
